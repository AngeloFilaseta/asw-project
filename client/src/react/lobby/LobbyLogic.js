import $ from "jquery"

export function sendMsg(socket, username, lobbyCode, msg) {
    /*var msgBody = {
        username: username,
        code: lobbyCode,
        msg: msg
    }
    $("#chatSendButton").val('');
    eventbus.send('chat', msgBody);*/
}